##Homework 02
#Question 1

araonavowels <- read.csv("/Users/Adan Tallman/Desktop/vowels2.csv", header=TRUE) 

head(araonavowels)

# Calculate standard deviation
duration_sd <- sd(araonavowels$duration)
# Calculate standard error
duration_se <- duration_sd / sqrt(length(araonavowels$duration))
# Calculate standard deviation
intensity_sd <- sd(araonavowels$intensity)
# Calculate standard error
intensity_se <- intensity_sd / sqrt(length(araonavowels$intensity))

##Question 2
#Base R
par(mfrow=c(1,2))
araona_stressed <- araonavowels[araonavowels$stress=="y",]
araona_unstressed <- araonavowels[araonavowels$stress=="n",]
boxplot(araona_stressed$duration)
boxplot(araona_unstressed$duration)


##Tidyverse / ggplot
library(tidyverse)
ggplot(araonavowels, aes(y=duration, x=stress))+
  geom_boxplot()

##Question 3
#Base R
par(mfrow=c(1,1))
frequency_table <- table(araonavowels$stress)
barplot(frequency_table)

#In ggplot
ggplot(araonavowels, aes(x = stress, fill = stress)) +
  geom_bar() +
  scale_fill_manual(values = c("skyblue", "salmon")) +
  theme_minimal()

##Question 4
set.seed(123)
group1 <- rnorm(m=2.8, sd=6,n=40)
group2 <- rnorm(m=0, sd=6,n=40)
t.test(group1, group2)

# Create density estimates for each group
density_group1 <- density(group1)
density_group2 <- density(group2)
plot(density_group2, col = "blue")
lines(density_group1, col = "red")

# Combine data into a data frame
df <- data.frame(
  value = c(group1, group2),
  group = rep(c("Group 1", "Group 2"), each = length(group1))
)

ggplot(df, aes(x = value, fill = group)) +
  geom_density(alpha = 0.5) +
  labs(title = "Density Plot of Two Groups", x = "Value") +
  scale_fill_manual(values = c("blue", "red")) +
  theme_minimal()

##Question 5
chacobo.mcf <- read.csv("/Users/Adan Tallman/Desktop/chacobo.mcf.df.csv", header=TRUE) 
head(chacobo.mcf)
##(i) pitch
ggplot(chacobo.mcf, aes(y=pitch.f0, x=response))+
  geom_boxplot()+
  facet_wrap(~subject)
##(ii) duration
ggplot(chacobo.mcf, aes(y=duration.ms, x=response))+
  geom_boxplot()+
  facet_wrap(~subject)
##(iii)
ggplot(chacobo.mcf, aes(y=reactionTime, x=as.character(duration.ms)))+
  geom_boxplot()+
  facet_wrap(~subject)+
  ylim(0,10)
##(iii)
ggplot(chacobo.mcf, aes(y=reactionTime, x=as.character(pitch.f0)))+
  geom_boxplot()+
  facet_wrap(~subject)+
  ylim(0,10)
#(iv) - PYR
